# Pengzi 3D Preview

手机和网页 3D 预览文件。

入口文件：`index.html`
模型文件：`steel_structure_cloud_view_lowpoly_draco.glb`

## 本地查看

不要直接双击 `index.html`。浏览器会拦截本地 `.glb` 模型加载。

Windows 下双击：

`start_local_preview.bat`

然后会自动打开：

`http://127.0.0.1:8088/index.html`

## 服务器部署

部署到服务器网站目录后，直接访问 `index.html`。
